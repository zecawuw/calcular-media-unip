import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

    resultNota: {
        flex: 1,
        marginTop: 15,
        paddingTop: 60,
        borderRadius: 50,
        alignItems: 'center',
        width: '100%',
    },

    numberNota:{
        fontSize: 48,
        color: '#1e53a8',
        fontWeight: 'bold',
    },

    information: {
        fontSize: 18,
        color: '#1e53a8',
        fontWeight: 'bold',
    },
  
});

export default styles