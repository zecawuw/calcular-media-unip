import { Text, View } from 'react-native';
import styles from './style';

export default function Titlel() {
  return (
    <View style={styles.boxTitle}>
      <Text style={styles.textTitle}>CALCULAR NOTA</Text>
    </View>
  );
}

